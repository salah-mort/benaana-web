# دليل تشغيل مكتبة الملفات — سند وعمار

## ما تم دمجه

تم تحويل المشروع إلى Full-Stack باستخدام المصادقة وقاعدة البيانات، وأضيفت مكتبة ملفات داخل تجربة الدفتر. تُحفظ محتويات الملفات في التخزين السحابي، بينما تحفظ قاعدة البيانات البيانات الوصفية فقط: اسم الملف، النوع، الحجم، رابط التخزين، المالك، والتاريخ.

## طريقة الاستخدام

1. افتح موقع سند وعمار واضغط على زر **دخول**.
2. أكمل تسجيل الدخول عبر بوابة OAuth.
3. افتح **مكتبة الخيط** من شريط الأدوات أو من المشهد المخصص لها.
4. اضغط **ارفع ملفاً** واختر ملفاً مسموحاً به.
5. بعد نجاح الرفع ستظهر بطاقة الملف مع خياري **فتح** و**تنزيل**، إضافة إلى زر الحذف.
6. الملفات مرتبطة بحساب المستخدم؛ لا يستطيع مستخدم رؤية ملفات مستخدم آخر أو حذفها.

## القيود الأمنية الحالية

الرفع محصور بالملفات والأنواع والحجم المحدد في طبقة التحقق المشتركة. لا تُحفظ بايتات الملفات داخل قاعدة البيانات. عمليات القائمة والرفع والحذف محمية بالمصادقة وملكية المستخدم.

## ملاحظة الاختبار

نجحت اختبارات Vitest الخاصة بتنظيف أسماء الملفات، حدود الحجم والأنواع، منع الوصول دون تسجيل دخول، ودورة الرفع والقائمة والحذف مع عزل الملكية بين مستخدمين. لم تُنفذ جلسة OAuth يدوية داخل المعاينة الآلية؛ إذ ظهرت فيها رسائل **Missing session cookie**، لذلك يجب تنفيذ اختبار عملي أخير من واجهة المعاينة بعد تسجيل دخول مستخدم فعلي للتأكد من الرفع والتنزيل في بيئة الجلسة.

## الاختبارات البرمجية

- `pnpm test`: 4 ملفات اختبار، 7 اختبارات ناجحة.
- `pnpm check`: ناجح دون أخطاء TypeScript.
- `pnpm build`: ناجح. يظهر تحذير حجم حزمة JavaScript فقط، وليس فشل بناء.
